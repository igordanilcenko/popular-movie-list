<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
        xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:tools="http://schemas.android.com/tools"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        tools:listitem="@layout/item_movie_detail"
        >

    <TextView
            android:id="@+id/item_number"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginStart="@dimen/global_margin_m"
            android:layout_marginTop="@dimen/global_margin_m"
            android:layout_marginEnd="@dimen/global_margin_m"
            android:layout_marginBottom="@dimen/global_margin_s"
            android:text="@string/movie_detail_item_similar_movies_title"
            android:textAppearance="?attr/textAppearanceOverline"
            />

    <androidx.recyclerview.widget.RecyclerView
            android:id="@+id/recycler_view_similar_movies"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            tools:listitem="@layout/item_similar_movie"
            />
</LinearLayout>
